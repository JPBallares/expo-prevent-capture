{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "paths": {
      "expo-prevent-capture": ["../src/index"],
      "expo-prevent-capture/*": ["../src/*"]
    }
  }
}
